<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:services="clr-namespace:SimpleForum.Client.Services;assembly=SimpleForum.Client"
             x:Class="SimpleForum.Client.Views.MainPage">

    <StackLayout>
        <ListView ItemsSource="{Binding Source={x:Static services:AccountService.Accounts}}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Button Text="{Binding Fullname}" />
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView> 
        <Button Text="Add account" Clicked="AddAccount" />
    </StackLayout>

</ContentPage>
