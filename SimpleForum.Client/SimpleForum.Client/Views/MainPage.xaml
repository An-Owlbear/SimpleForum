<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:SimpleForum.Client.ViewModels;assembly=SimpleForum.Client"
             xmlns:models="clr-namespace:SimpleForum.Client.Models;assembly=SimpleForum.Client"
             x:Class="SimpleForum.Client.Views.MainPage" x:DataType="viewModels:MainViewModel">

    <StackLayout>
        <ListView ItemsSource="{Binding AccountService.Accounts}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Account">
                    <ViewCell>
                        <Button Text="{Binding Fullname}" Command="{Binding UseUserCommand}" />
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView> 
        <Button Text="Add account" Clicked="AddAccount" />
    </StackLayout>

</ContentPage>
